# Server Configuration
server.port=8080

# --- Database Selection ---
# Uncomment the section you want to use (MySQL or H2). Defaulting to H2 for easy testing.

# =========================================================
# OPTION 1: H2 Configuration (In-Memory, No Setup Required)
# =========================================================
# spring.datasource.url=jdbc:h2:mem:homease;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true
# spring.jpa.hibernate.ddl-auto=update

# =========================================================
# OPTION 2: MySQL Configuration (Requires MySQL Server)
# =========================================================
spring.datasource.url=jdbc:mysql://localhost:3306/homease
spring.datasource.username=root
spring.datasource.password=Lokesh@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update

# Logging
logging.level.org.springframework.web=INFO
spring.jpa.show-sql=true

# Email Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=homeservices075@gmail.com
spring.mail.password=ssaj yrcx xegw hkeb
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true